<Window x:Class="WpfUI.Views.Windows.RatesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:WpfUI.Views.Controls"
        xmlns:converters="clr-namespace:WpfUI.Converters"
        Title="Energy Rates"
        Height="800" Width="1254"
        ShowInTaskbar="False">

    <DockPanel>

        <DockPanel.Resources>
            <converters:DecimalInputConverter x:Key="DecimalInputConverter"/>
        </DockPanel.Resources>

        <!-- Toolbar -->
        <ToolBar DockPanel.Dock="Top">
            <Button Command="{Binding AddRateCommand}">
                <StackPanel Orientation="Horizontal">
                    <Image Width="20" Height="20" Source="pack://application:,,,/WpfUI;component/Resources/add_24x24.png"/>
                    <TextBlock Text="Add" Margin="5 0 0 0"/>
                </StackPanel>
            </Button>

            <Button Command="{Binding SaveRateCommand}">
                <StackPanel Orientation="Horizontal">
                    <Image Width="20" Height="20" Source="pack://application:,,,/WpfUI;component/Resources/tick-mark_24x24.png"/>
                    <TextBlock Text="Save" Margin="5 0 0 0"/>
                </StackPanel>
            </Button>

            <Button Command="{Binding CancelRateCommand}">
                <StackPanel Orientation="Horizontal">
                    <Image Width="20" Height="20" Source="pack://application:,,,/WpfUI;component/Resources/clear_24x24.png"/>
                    <TextBlock Text="Cancel" Margin="5 0 0 0"/>
                </StackPanel>
            </Button>

            <Button Command="{Binding DeleteRateCommand}">
                <StackPanel Orientation="Horizontal">
                    <Image Width="20" Height="20" Source="pack://application:,,,/WpfUI;component/Resources/delete_24x24.png"/>
                    <TextBlock Text="Delete" Margin="5 0 0 0"/>
                </StackPanel>
            </Button>

            <Button Command="{Binding RefreshRatesCommand}">
                <StackPanel Orientation="Horizontal">
                    <Image Width="20" Height="20" Source="pack://application:,,,/WpfUI;component/Resources/clock_24x24.png"/>
                    <TextBlock Text="Refresh" Margin="5 0 0 0"/>
                </StackPanel>
            </Button>

            <Button Command="{Binding CloseCommand}">
                <StackPanel Orientation="Horizontal">
                    <Image Width="20" Height="20" Source="pack://application:,,,/WpfUI;component/Resources/crossed_24x24.png"/>
                    <TextBlock Text="Close" Margin="5 0 0 0"/>
                </StackPanel>
            </Button>
        </ToolBar>

        <!-- Statusbar -->
        <StatusBar DockPanel.Dock="Bottom">
            <StatusBarItem>
                <TextBlock Text="{Binding RateTaxInfoText}" />
            </StatusBarItem>
        </StatusBar>

        <!-- Main Layout -->
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <!-- Filters -->
                <RowDefinition Height="*"/>
                <!-- Content -->
            </Grid.RowDefinitions>

            <!-- FILTERS -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">

                <StackPanel Margin="0,0,20,0">
                    <TextBlock Text="Energy Type"/>
                    <ComboBox ItemsSource="{Binding EnergyTypes}"
                              SelectedItem="{Binding SelectedEnergyType}"
                              DisplayMemberPath="Name"
                              Width="180"/>
                </StackPanel>

                <StackPanel Margin="0,0,20,0">
                    <TextBlock Text="Cost Category"/>
                    <ComboBox ItemsSource="{Binding CostCategories}"
                              SelectedItem="{Binding SelectedCostCategory}"
                              DisplayMemberPath="Name"
                              Width="180"/>
                </StackPanel>

                <StackPanel Margin="0,0,20,0">
                    <TextBlock Text="Tariff Group"/>
                    <ComboBox ItemsSource="{Binding TariffGroups}"
                              SelectedItem="{Binding SelectedTariffGroup}"
                              DisplayMemberPath="Description"
                              Width="180"/>
                </StackPanel>

                <TextBlock Text="{Binding AlwaysCalculatedWithText}"
                           VerticalAlignment="Bottom"
                           Margin="20,0,0,0"
                           FontStyle="Italic">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsAlwaysCalculatedWithVisible}" Value="True">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>

            </StackPanel>

            <!-- CONTENT: LEFT = Rates + Staffel, RIGHT = Details -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <!-- Left -->
                    <ColumnDefinition Width="420"/>
                    <!-- Right -->
                </Grid.ColumnDefinitions>

                <!-- LEFT SIDE -->
                <Grid Grid.Column="0" Margin="0,0,10,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="2*"/>
                        <!-- Rates -->
                        <RowDefinition Height="*"/>
                        <!-- Staffel -->
                    </Grid.RowDefinitions>

                    <!-- RATES GRID -->
                    <DataGrid Grid.Row="0"
                              ItemsSource="{Binding RateRows}"
                              SelectedItem="{Binding SelectedRateRow}"
                              AutoGenerateColumns="False"
                              Margin="0,0,0,10">

                        <DataGrid.Resources>
                            <Style x:Key="NumericCellStyle" TargetType="TextBlock">
                                <Setter Property="TextAlignment" Value="Right"/>
                                <Setter Property="Padding" Value="0,0,4,0"/>
                            </Style>
                        </DataGrid.Resources>

                        <DataGrid.ColumnHeaderStyle>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                <Setter Property="VerticalContentAlignment" Value="Center"/>
                                <Setter Property="ContentTemplate">
                                    <Setter.Value>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding}"
                                                       TextWrapping="Wrap"
                                                       TextAlignment="Center"/>
                                        </DataTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </DataGrid.ColumnHeaderStyle>

                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Start"
                                                Width="*"
                                                Binding="{Binding StartRate, StringFormat=\{0:dd-MM-yyyy\}}"/>
                            <DataGridTextColumn Header="End"
                                                Width="*"
                                                Binding="{Binding EndRate, StringFormat=\{0:dd-MM-yyyy\}}"/>
                            <DataGridTextColumn Header="Rate"
                                                Width="0.9*"
                                                ElementStyle="{StaticResource NumericCellStyle}"
                                                Binding="{Binding RateValue, StringFormat=N5}" />
                            <DataGridTextColumn Header="Price incl. VAT"
                                                Width="0.9*"
                                                ElementStyle="{StaticResource NumericCellStyle}"
                                                Binding="{Binding RateIncludingVat, StringFormat=N5}" />
                            <DataGridTextColumn Header="Expected Change"
                                                Width="0.75*"
                                                ElementStyle="{StaticResource NumericCellStyle}"
                                                Binding="{Binding ExpectedPriceChange, StringFormat=\{0:N2\}%}" />
                            <DataGridTextColumn Header="Price Change"
                                                Width="0.75*"
                                                ElementStyle="{StaticResource NumericCellStyle}"
                                                Binding="{Binding PriceChange, StringFormat=\{0:N2\}%}" />
                            <DataGridTextColumn Header="Future excl. VAT"
                                                Width="0.9*"
                                                ElementStyle="{StaticResource NumericCellStyle}"
                                                Binding="{Binding FutureRateExcludingVat, StringFormat=N5}" />
                            <DataGridTextColumn Header="Future incl. VAT"
                                                Width="0.9*"
                                                ElementStyle="{StaticResource NumericCellStyle}"
                                                Binding="{Binding FutureRateIncludingVat, StringFormat=N5}" />
                            <DataGridTextColumn Header="Description"
                                                Width="2*"
                                                Binding="{Binding Description}" />
                        </DataGrid.Columns>
                    </DataGrid>

                    <!-- STAFFEL GRID -->
                    <GroupBox Grid.Row="1" Header="Staffel">
                        <controls:StaffelControl DataContext="{Binding StaffelVM}">
                            <controls:StaffelControl.Style>
                                <Style TargetType="controls:StaffelControl">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding DataContext.IsStaffel,
                                                               RelativeSource={RelativeSource AncestorType=Window}}"
                                                     Value="True">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </controls:StaffelControl.Style>
                        </controls:StaffelControl>
                    </GroupBox>
                </Grid>

                <!-- RIGHT SIDE: RATE DETAILS -->
                <GroupBox Grid.Column="1" Header="Rate Details">
                    <Grid Margin="10">

                        <Grid.Resources>

                            <!-- Labels -->
                            <Style x:Key="DetailLabelStyle" TargetType="TextBlock">
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="Margin" Value="0,4"/>
                                <Setter Property="FontSize" Value="14"/>
                            </Style>

                            <!-- TextBox -->
                            <Style x:Key="DetailTextBoxStyle" TargetType="TextBox">
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="Margin" Value="0,4"/>
                                <Setter Property="Height" Value="26"/>
                                <Setter Property="FontSize" Value="14"/>
                            </Style>

                            <!-- ComboBox -->
                            <Style x:Key="DetailComboBoxStyle" TargetType="ComboBox">
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="Margin" Value="0,4"/>
                                <Setter Property="Height" Value="26"/>
                                <Setter Property="FontSize" Value="14"/>
                            </Style>

                            <!-- DatePicker -->
                            <Style x:Key="DetailDatePickerStyle" TargetType="DatePicker">
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="Margin" Value="0,4"/>
                                <Setter Property="Height" Value="26"/>
                                <Setter Property="FontSize" Value="14"/>
                            </Style>

                        </Grid.Resources>

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <!-- Rate Type -->
                            <RowDefinition Height="Auto"/>
                            <!-- Start -->
                            <RowDefinition Height="Auto"/>
                            <!-- End -->
                            <RowDefinition Height="Auto"/>
                            <!-- Rate -->
                            <RowDefinition Height="Auto"/>
                            <!-- Expected -->
                            <RowDefinition Height="Auto"/>
                            <!-- Price Change -->
                            <RowDefinition Height="Auto"/>
                            <!-- Future excluding VAT -->
                            <RowDefinition Height="Auto"/>
                            <!-- Future including VAT -->
                            <RowDefinition Height="Auto"/>
                            <!-- Description -->
                        </Grid.RowDefinitions>

                        <!-- Rate Type -->
                        <TextBlock Grid.Row="0" Text="Rate Type"
                                   Style="{StaticResource DetailLabelStyle}"/>
                        <ComboBox Grid.Row="0" Grid.Column="1"
                                  ItemsSource="{Binding RateTypes}"
                                  SelectedItem="{Binding SelectedRateType}"
                                  DisplayMemberPath="Description"
                                  Style="{StaticResource DetailComboBoxStyle}"/>

                        <!-- Start Date -->
                        <TextBlock Grid.Row="1" Text="Start Date"
                                   Style="{StaticResource DetailLabelStyle}"/>
                        <DatePicker Grid.Row="1" Grid.Column="1"
                                    SelectedDate="{Binding SelectedRate.StartRate}"
                                    Style="{StaticResource DetailDatePickerStyle}"/>

                        <!-- End Date -->
                        <TextBlock Grid.Row="2" Text="End Date"
                                   Style="{StaticResource DetailLabelStyle}"/>
                        <DatePicker Grid.Row="2" Grid.Column="1"
                                    SelectedDate="{Binding SelectedRate.EndRate}"
                                    Style="{StaticResource DetailDatePickerStyle}"/>

                        <!-- Rate -->
                        <TextBlock Grid.Row="3" Text="Rate"
                                   Style="{StaticResource DetailLabelStyle}"/>
                        <TextBox Grid.Row="3" Grid.Column="1"
                                 Text="{Binding SelectedRate.RateValue,
                                                Converter={StaticResource DecimalInputConverter},
                                                UpdateSourceTrigger=PropertyChanged}">
                            <TextBox.Style>
                                <Style TargetType="TextBox" BasedOn="{StaticResource DetailTextBoxStyle}">
                                    <Setter Property="Visibility" Value="Visible"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsStaffel}" Value="True">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBox.Style>
                        </TextBox>

                        <!-- Expected Price Change -->
                        <TextBlock Grid.Row="4" Text="Expected Price Change"
                                   Style="{StaticResource DetailLabelStyle}"/>
                        <TextBox Grid.Row="4" Grid.Column="1"
                                 Text="{Binding SelectedRate.ExpectedPriceChange,
                                                Converter={StaticResource DecimalInputConverter},
                                                UpdateSourceTrigger=PropertyChanged}"
                                 Style="{StaticResource DetailTextBoxStyle}"/>

                        <!-- Price Change -->
                        <TextBlock Grid.Row="5" Text="Price Change"
                                   Style="{StaticResource DetailLabelStyle}"/>
                        <TextBox Grid.Row="5" Grid.Column="1"
                                 Text="{Binding SelectedRate.PriceChange}"
                                 IsReadOnly="True"
                                 Style="{StaticResource DetailTextBoxStyle}"/>

                        <!-- Future Price excluding VAT -->
                        <TextBlock Grid.Row="6" Text="Future Price excl. VAT"
                                   Style="{StaticResource DetailLabelStyle}"/>
                        <TextBlock Grid.Row="6" Grid.Column="1"
                                   Text="{Binding SelectedRateRow.FutureRateExcludingVat, StringFormat=N5}"
                                   Style="{StaticResource DetailLabelStyle}"/>

                        <!-- Future Price including VAT -->
                        <TextBlock Grid.Row="7" Text="Future Price incl. VAT"
                                   Style="{StaticResource DetailLabelStyle}"/>
                        <TextBlock Grid.Row="7" Grid.Column="1"
                                   Text="{Binding SelectedRateRow.FutureRateIncludingVat, StringFormat=N5}"
                                   Style="{StaticResource DetailLabelStyle}"/>

                        <!-- Description -->
                        <TextBlock Grid.Row="8" Text="Description"
                                   Style="{StaticResource DetailLabelStyle}"/>
                        <TextBox Grid.Row="8" Grid.Column="1"
                                 Text="{Binding SelectedRate.Description}"
                                 Style="{StaticResource DetailTextBoxStyle}"/>

                    </Grid>
                </GroupBox>

            </Grid>
        </Grid>
    </DockPanel>
</Window>
