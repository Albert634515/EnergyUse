<Window x:Class="WpfUI.Views.Windows.RatesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:WpfUI.Views.Controls"
        Title="Energy Rates"
        Height="800" Width="850"
        ShowInTaskbar="False">

    <DockPanel>

        <!-- Toolbar -->
        <ToolBar DockPanel.Dock="Top">

            <Button Command="{Binding AddRateCommand}">
                <StackPanel Orientation="Horizontal">
                    <Image Source="/Resources/add_24x24.png" Width="20" Height="20"/>
                    <TextBlock Text="Add" Margin="5,0,0,0"/>
                </StackPanel>
            </Button>

            <Button Command="{Binding SaveRateCommand}">
                <StackPanel Orientation="Horizontal">
                    <Image Source="/Resources/tick-mark_24x24.png" Width="20" Height="20"/>
                    <TextBlock Text="Save" Margin="5,0,0,0"/>
                </StackPanel>
            </Button>

            <Button Command="{Binding CancelRateCommand}">
                <StackPanel Orientation="Horizontal">
                    <Image Source="/Resources/clear_24x24.png" Width="20" Height="20"/>
                    <TextBlock Text="Cancel" Margin="5,0,0,0"/>
                </StackPanel>
            </Button>

            <Button Command="{Binding DeleteRateCommand}">
                <StackPanel Orientation="Horizontal">
                    <Image Source="/Resources/delete_24x24.png" Width="20" Height="20"/>
                    <TextBlock Text="Delete" Margin="5,0,0,0"/>
                </StackPanel>
            </Button>

            <Button Command="{Binding RefreshRatesCommand}">
                <StackPanel Orientation="Horizontal">
                    <Image Source="/Resources/clock_24x24.png" Width="20" Height="20"/>
                    <TextBlock Text="Refresh" Margin="5,0,0,0"/>
                </StackPanel>
            </Button>

            <Button Command="{Binding CloseCommand}">
                <StackPanel Orientation="Horizontal">
                    <Image Source="/Resources/crossed_24x24.png" Width="20" Height="20"/>
                    <TextBlock Text="Close" Margin="5,0,0,0"/>
                </StackPanel>
            </Button>

        </ToolBar>

        <!-- Statusbar -->
        <StatusBar DockPanel.Dock="Bottom">
            <StatusBarItem>
                <TextBlock Text="{Binding RateTaxInfoText}" />
            </StatusBarItem>
        </StatusBar>

        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Filters -->
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">

                <StackPanel Margin="0,0,20,0">
                    <TextBlock Text="Energy Type"/>
                    <ComboBox ItemsSource="{Binding EnergyTypes}"
                              SelectedItem="{Binding SelectedEnergyType}"
                              DisplayMemberPath="Name"
                              Width="180"/>
                </StackPanel>

                <StackPanel Margin="0,0,20,0">
                    <TextBlock Text="Cost Category"/>
                    <ComboBox ItemsSource="{Binding CostCategories}"
                              SelectedItem="{Binding SelectedCostCategory}"
                              DisplayMemberPath="Name"
                              Width="180"/>
                </StackPanel>

                <StackPanel Margin="0,0,20,0">
                    <TextBlock Text="Tariff Group"/>
                    <ComboBox ItemsSource="{Binding TariffGroups}"
                              SelectedItem="{Binding SelectedTariffGroup}"
                              DisplayMemberPath="Description"
                              Width="180"/>
                </StackPanel>

                <!-- AlwaysCalculatedWith -->
                <TextBlock Text="{Binding AlwaysCalculatedWithText}"
                           VerticalAlignment="Bottom"
                           Margin="20,0,0,0"
                           FontStyle="Italic">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsAlwaysCalculatedWithVisible}" Value="True">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>

            </StackPanel>

            <!-- Rates Grid -->
            <DataGrid Grid.Row="1"
                      ItemsSource="{Binding Rates}"
                      SelectedItem="{Binding SelectedRate}"
                      AutoGenerateColumns="False"
                      IsReadOnly="False"
                      Margin="0,0,0,10">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Start" Binding="{Binding StartRate}" />
                    <DataGridTextColumn Header="End" Binding="{Binding EndRate}" />
                    <DataGridTextColumn Header="Rate" Binding="{Binding RateValue, StringFormat=N5}" />
                    <DataGridTextColumn Header="Expected Change" Binding="{Binding ExpectedPriceChange, StringFormat=N3}" />
                    <DataGridTextColumn Header="Price Change" Binding="{Binding PriceChange, StringFormat=N3}" />
                    <DataGridTextColumn Header="Description" Binding="{Binding Description}" />
                </DataGrid.Columns>
            </DataGrid>

            <!-- Rate Details -->
            <GroupBox Grid.Row="2" Header="Rate Details">
                <Grid Margin="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="200"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Rate Type -->
                    <TextBlock Grid.Row="0" Text="Rate Type" VerticalAlignment="Center"/>
                    <ComboBox Grid.Row="0" Grid.Column="1"
                              ItemsSource="{Binding RateTypes}"
                              SelectedItem="{Binding SelectedRateType}"
                              DisplayMemberPath="Description"
                              Width="200"/>

                    <!-- Rate -->
                    <TextBlock Grid.Row="1" Text="Rate" VerticalAlignment="Center"/>
                    <TextBox Grid.Row="1" Grid.Column="1"
                             Text="{Binding SelectedRate.RateValue, UpdateSourceTrigger=PropertyChanged}">
                        <TextBox.Style>
                            <Style TargetType="TextBox">
                                <Setter Property="Visibility" Value="Visible"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsStaffel}" Value="True">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBox.Style>
                    </TextBox>

                    <!-- Expected Price Change -->
                    <TextBlock Grid.Row="2" Text="Expected Price Change" VerticalAlignment="Center"/>
                    <TextBox Grid.Row="2" Grid.Column="1"
                             Text="{Binding SelectedRate.ExpectedPriceChange, UpdateSourceTrigger=PropertyChanged}"/>

                    <!-- Price Change -->
                    <TextBlock Grid.Row="3" Text="Price Change" VerticalAlignment="Center"/>
                    <TextBox Grid.Row="3" Grid.Column="1"
                             Text="{Binding SelectedRate.PriceChange}"
                             IsReadOnly="True"/>

                    <!-- Future Price -->
                    <TextBlock Grid.Row="4" Text="Future Price" VerticalAlignment="Center"/>
                    <TextBlock Grid.Row="4" Grid.Column="1"
                               Text="{Binding SelectedRate.FutureRate}"/>

                    <!-- Description -->
                    <TextBlock Grid.Row="5" Text="Description" VerticalAlignment="Center"/>
                    <TextBox Grid.Row="5" Grid.Column="1"
                             Text="{Binding SelectedRate.Description}"/>

                    <!-- Staffel Control -->
                    <controls:StaffelControl Grid.Row="6" Grid.ColumnSpan="2"
                                             RateId="{Binding SelectedRate.Id}">
                        <controls:StaffelControl.Style>
                            <Style TargetType="controls:StaffelControl">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsStaffel}" Value="True">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </controls:StaffelControl.Style>
                    </controls:StaffelControl>

                </Grid>
            </GroupBox>

        </Grid>
    </DockPanel>
</Window>