<Window x:Class="WpfUI.Views.Windows.PayBackTimeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:WpfUI.Views.Controls"
        Title="Payback Time"
        Height="800" Width="1200"
        ShowInTaskbar="False">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </Window.Resources>

    <DockPanel>

        <!-- ⭐ ACTIEKNOPPEN -->
        <ToolBar DockPanel.Dock="Top">
            <Button Command="{Binding CalculateCommand}">
                <StackPanel Orientation="Horizontal">
                    <Image Source="/Resources/calculator_24x24.png" Width="20" Height="20"/>
                    <TextBlock Text="Calculate" Margin="6,0,0,0"/>
                </StackPanel>
            </Button>

            <Button Command="{Binding CloseCommand}">
                <StackPanel Orientation="Horizontal">
                    <Image Source="/Resources/crossed_24x24.png" Width="20" Height="20"/>
                    <TextBlock Text="Close" Margin="6,0,0,0"/>
                </StackPanel>
            </Button>
        </ToolBar>

        <!-- ⭐ STATUSBALK -->
        <StatusBar DockPanel.Dock="Bottom">
            <StatusBarItem>
                <ProgressBar Width="200"
                             Height="16"
                             Minimum="0"
                             Maximum="100"
                             Value="{Binding ProgressValue}"
                             Visibility="{Binding IsProgressVisible, Converter={StaticResource BooleanToVisibilityConverter}}"/>
            </StatusBarItem>
            <StatusBarItem>
                <TextBlock Text="{Binding ProgressText}"/>
            </StatusBarItem>
        </StatusBar>

        <!-- ⭐ HOOFDLAYOUT -->
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- ⭐ FORM FIELDS -->
            <StackPanel Grid.Row="0" Orientation="Horizontal">

                <StackPanel Margin="0,0,20,0">
                    <TextBlock Text="Address"/>
                    <ComboBox ItemsSource="{Binding Addresses}"
                              SelectedItem="{Binding SelectedAddress}"
                              DisplayMemberPath="Description"
                              Width="200"/>
                </StackPanel>

                <StackPanel Margin="0,0,20,0">
                    <TextBlock Text="Energy Type"/>
                    <ComboBox ItemsSource="{Binding EnergyTypes}"
                              SelectedItem="{Binding SelectedEnergyType}"
                              DisplayMemberPath="Name"
                              Width="200"/>
                </StackPanel>

                <StackPanel Margin="0,0,20,0">
                    <TextBlock Text="Purchase Date"/>
                    <DatePicker SelectedDate="{Binding PurchaseDate}" Width="150"/>
                </StackPanel>

                <StackPanel Margin="0,0,20,0">
                    <TextBlock Text="Max Years"/>
                    <controls:NumericUpDown Value="{Binding MaxYears}" Width="80"/>
                </StackPanel>

            </StackPanel>

            <!-- ⭐ FINANCIAL INPUTS -->
            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,20,0,20">

                <StackPanel Margin="0,0,20,0">
                    <TextBlock Text="Purchase Amount"/>
                    <TextBox Text="{Binding PurchaseAmount}" Width="120"/>
                </StackPanel>

                <StackPanel Margin="0,0,20,0">
                    <TextBlock Text="Subsidy Amount"/>
                    <TextBox Text="{Binding SubsidyAmount}" Width="120"/>
                </StackPanel>

                <StackPanel Margin="0,0,20,0">
                    <TextBlock Text="Quality Reduction"/>
                    <TextBox Text="{Binding QualityReduction}" Width="120"/>
                </StackPanel>

                <StackPanel Margin="0,0,20,0">
                    <TextBlock Text="Average Return"/>
                    <TextBox Text="{Binding AverageReturn}" Width="120"/>
                </StackPanel>

                <StackPanel Margin="0,0,20,0">
                    <TextBlock Text="Total Capacity"/>
                    <TextBox Text="{Binding TotalCapacity}" Width="120" IsReadOnly="True"/>
                </StackPanel>

            </StackPanel>

            <!-- ⭐ DATAGRID -->
            <DataGrid Grid.Row="2"
                      ItemsSource="{Binding PayBackTimes}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True">

                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Height" Value="Auto"/>
                        <Setter Property="Padding" Value="6"/>
                        <Setter Property="VerticalContentAlignment" Value="Center"/>
                        <Setter Property="HorizontalContentAlignment" Value="Center"/>
                        <Setter Property="ContentTemplate">
                            <Setter.Value>
                                <DataTemplate>
                                    <TextBlock Text="{Binding}"
                               TextWrapping="Wrap"
                               TextAlignment="Center"/>
                                </DataTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </DataGrid.ColumnHeaderStyle>

                <!-- ⭐ KLEURLOGICA ZONDER CONVERTER -->
                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Setter Property="Background" Value="{Binding RoiColor}"/>
                        <Setter Property="Foreground" Value="{Binding RoiForeground}"/>
                    </Style>
                </DataGrid.RowStyle>

                <DataGrid.Columns>

                    <DataGridTextColumn Header="Period" Width="50" Binding="{Binding PeriodId}"/>
                    <DataGridTextColumn Header="Start" Width="80" Binding="{Binding StartPeriod, StringFormat={}{0:dd-MM-yyyy}}"/>
                    <DataGridTextColumn Header="End" Width="80" Binding="{Binding EndPeriod, StringFormat={}{0:dd-MM-yyyy}}"/>

                    <DataGridTextColumn Header="Produced" Binding="{Binding ValueProduced, StringFormat=N2}"/>
                    <DataGridTextColumn Header="Consumed" Binding="{Binding ValueConsumed, StringFormat=N2}"/>

                    <DataGridTextColumn Header="Estimate Direct Used" Width="100" Binding="{Binding EstimateDirectUsed, StringFormat=N2}"/>
                    <DataGridTextColumn Header="Estimate Direct Used perc." Width="90" Binding="{Binding EstimateDirectUsedPercentage, StringFormat=N0}"/>
                    <DataGridTextColumn Header="Monetary Produced" Width="100" Binding="{Binding MonetaryValueProduced, StringFormat=N2}"/>
                    <DataGridTextColumn Header="Produced Est. Direct Used" Width="100" Binding="{Binding ValueProducedEstimateDirectUsed, StringFormat=N2}"/>
                    <DataGridTextColumn Header="Monetary Consumed" Width="100" Binding="{Binding MonetaryValueConsumed, StringFormat=N2}"/>

                    <DataGridTextColumn Header="Netto Produced" Width="100" Binding="{Binding NettoProduced, StringFormat=N2}"/>
                    <DataGridTextColumn Header="Other Cost Produced" Width="100" Binding="{Binding OtherCostProduced, StringFormat=N2}"/>
                    <DataGridTextColumn Header="Other Cost Consumed" Width="100" Binding="{Binding OtherCostConsumed, StringFormat=N2}"/>

                    <DataGridTextColumn Header="Total Cost" Width="70" Binding="{Binding TotalCost, StringFormat=N2}"/>
                    <DataGridTextColumn Header="ROI" Width="70" Binding="{Binding ReturnOnInvestment, StringFormat=N2}"/>
                    <DataGridTextColumn Header="ROI Total" Width="80" Binding="{Binding ReturnOnInvestmentTotal, StringFormat=N2}"/>
                    <DataGridTextColumn Header="Return" Width="60" Binding="{Binding Return, StringFormat=N2}"/>

                </DataGrid.Columns>

            </DataGrid>

        </Grid>

    </DockPanel>
</Window>