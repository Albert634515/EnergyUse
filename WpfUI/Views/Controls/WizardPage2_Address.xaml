<UserControl x:Class="WpfUI.Views.Controls.WizardPage2_Address"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="10">

            <TextBlock Text="Adresgegevens"
                       FontSize="20"
                       FontWeight="Bold"
                       Margin="0,0,0,20"/>

            <!-- Adres -->
            <GroupBox Header="Adres" FontSize="16">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="Straat:"/>
                    <TextBox Grid.Column="1" Margin="10,2"
                             Text="{Binding Address.Street}" />

                    <TextBlock Grid.Row="1" Text="Huisnummer:"/>
                    <TextBox Grid.Row="1" Grid.Column="1" Margin="10,2"
                             Text="{Binding Address.HouseNumber}" />

                    <TextBlock Grid.Row="2" Text="Plaats:"/>
                    <TextBox Grid.Row="2" Grid.Column="1" Margin="10,2"
                             Text="{Binding Address.City}" />

                    <TextBlock Grid.Row="3" Text="Postcode:"/>
                    <TextBox Grid.Row="3" Grid.Column="1" Margin="10,2"
                             Text="{Binding Address.PostalCode}" />
                </Grid>
            </GroupBox>

            <!-- Zonnepanelen -->
            <GroupBox Header="Zonnepanelen" FontSize="16" Margin="0,20,0,0">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="Aanwezig:"/>
                    <CheckBox Grid.Column="1" Margin="10,0"
                              IsChecked="{Binding Address.SolarPanelsAvailable}" />

                    <TextBlock Grid.Row="1" Text="Totale capaciteit (Wp):"/>
                    <TextBox Grid.Row="1" Grid.Column="1" Margin="10,2"
                             Text="{Binding Address.TotalCapacity}" />

                    <TextBlock Grid.Row="2" Text="Kwaliteitsreductie (%):"/>
                    <TextBox Grid.Row="2" Grid.Column="1" Margin="10,2"
                             Text="{Binding Address.QualityReductionSolarPanels}" />

                    <TextBlock Grid.Row="3" Text="Aanschafbedrag:"/>
                    <TextBox Grid.Row="3" Grid.Column="1" Margin="10,2"
                             Text="{Binding Address.PurchaseAmount}" />

                    <TextBlock Grid.Row="4" Text="Subsidie:"/>
                    <TextBox Grid.Row="4" Grid.Column="1" Margin="10,2"
                             Text="{Binding Address.SubsidyAmount}" />
                </Grid>
            </GroupBox>

            <!-- Beschrijving -->
            <GroupBox Header="Beschrijving" FontSize="16" Margin="0,20,0,0">
                <TextBox Margin="10"
                         TextWrapping="Wrap"
                         AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto"
                         Text="{Binding Address.Description}" />
            </GroupBox>

        </StackPanel>
    </ScrollViewer>
</UserControl>