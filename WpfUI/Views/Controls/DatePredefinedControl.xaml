<UserControl x:Class="WpfUI.Views.Controls.DatePredefinedControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <DockPanel>

        <!-- TOOLBAR -->
        <ToolBar DockPanel.Dock="Top">
            <Button Command="{Binding AddCommand}">
                <Image Source="/Resources/add_24x24.png" Width="20"/>
            </Button>
            <Button Command="{Binding SaveCommand}">
                <Image Source="/Resources/tick-mark_24x24.png" Width="20"/>
            </Button>
            <Button Command="{Binding CancelCommand}">
                <Image Source="/Resources/clear_24x24.png" Width="20"/>
            </Button>
            <Button Command="{Binding DeleteCommand}">
                <Image Source="/Resources/delete_24x24.png" Width="20"/>
            </Button>
            <Button Command="{Binding RefreshCommand}">
                <Image Source="/Resources/clock_24x24.png" Width="20"/>
            </Button>
        </ToolBar>

        <!-- MAIN CONTENT -->
        <Grid Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>

            <!-- DATAGRID -->
            <DataGrid Grid.Column="0"
                      ItemsSource="{Binding Dates}"
                      SelectedItem="{Binding SelectedDate, Mode=TwoWay}"
                      AutoGenerateColumns="False"
                      Margin="0,0,10,0">

                <DataGrid.Columns>
                    <DataGridTextColumn Header="Start" Binding="{Binding StartDate}" Width="120"/>
                    <DataGridTextColumn Header="End" Binding="{Binding EndDate}" Width="120"/>
                    <DataGridTextColumn Header="Energy Type" Binding="{Binding EnergyTypeName}" Width="150"/>
                    <DataGridTextColumn Header="Tariff Group" Binding="{Binding TarifGroupName}" Width="150"/>
                    <DataGridTextColumn Header="Id" Binding="{Binding Id}" Width="80"/>
                </DataGrid.Columns>
            </DataGrid>

            <!-- DETAILS -->
            <GroupBox Grid.Column="1" Header="Period date details">
                <Grid Margin="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="150"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Energy Type -->
                    <TextBlock Text="Energy type" Grid.Row="0" Grid.Column="0"/>
                    <ComboBox Grid.Row="0" Grid.Column="1" Margin="5"
                              ItemsSource="{Binding EnergyTypes}"
                              DisplayMemberPath="Name"
                              SelectedValuePath="Id"
                              SelectedValue="{Binding SelectedDate.EnergyTypeId, Mode=TwoWay}"/>

                    <!-- Tariff Group -->
                    <TextBlock Text="Tariff group" Grid.Row="1" Grid.Column="0"/>
                    <ComboBox Grid.Row="1" Grid.Column="1" Margin="5"
                              ItemsSource="{Binding TariffGroups}"
                              DisplayMemberPath="Description"
                              SelectedValuePath="Id"
                              SelectedValue="{Binding SelectedDate.TariffGroupId, Mode=TwoWay}"/>

                    <!-- Start Date -->
                    <TextBlock Text="Start date" Grid.Row="2" Grid.Column="0"/>
                    <DatePicker Grid.Row="2" Grid.Column="1" Margin="5"
                                SelectedDate="{Binding SelectedDate.StartDate, Mode=TwoWay}"/>

                    <!-- End Date -->
                    <TextBlock Text="End date" Grid.Row="3" Grid.Column="0"/>
                    <DatePicker Grid.Row="3" Grid.Column="1" Margin="5"
                                SelectedDate="{Binding SelectedDate.EndDate, Mode=TwoWay}"/>
                </Grid>
            </GroupBox>

        </Grid>
    </DockPanel>
</UserControl>